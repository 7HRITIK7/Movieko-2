{
  "name": "booking-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"cd api && node server.js\" \"cd src && npm start\"",
    "build": "cd src && npm run build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "express": "^4.17.1",
    "concurrently": "^6.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "engines": {
    "node": ">=14.x"
  }
}